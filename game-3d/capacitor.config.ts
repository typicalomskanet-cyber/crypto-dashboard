import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "ai.devin.chronicleofdevilgods",
  appName: "Chronicle of Devil Gods",
  webDir: "dist",
  server: { androidScheme: "https" },
};

export default config;
